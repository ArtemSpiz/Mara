/**
 * Tailwind CSS v4 — точка входу для конфігурації (плагіни, майбутні розширення).
 * Кольори, типографіка й розміри для утиліт задаються в @theme у app/globals.css
 * (див. docs/tailwind.md).
 */
const tailwindConfig = {};
export default tailwindConfig;
